tag-prefix: 'yr-'
mode: ContinuousDeployment
increment: Patch

branches:
  master:
    regex: ^master$
    source-branches: [ 'master' ]
    tag: 'dev'
    
  qm:
    regex: ^qm$
    source-branches: [ 'qm' ]
    tag: 'qm'
