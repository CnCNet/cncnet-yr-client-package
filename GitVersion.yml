label-prefix: 'yr-'
mode: ContinuousDeployment
increment: Patch

branches:
  develop:
    regex: ^develop
    source-branches: [ 'develop' ]
    label: 'dev'
    
  qm:
    regex: ^qm$
    source-branches: [ 'qm' ]
    label: 'qm'

  quakenet:
    regex: ^quakenet$
    source-branches: [ 'quakenet' ]
    label: 'quakenet'
