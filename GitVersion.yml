tag-prefix: 'yr-'
mode: ContinuousDeployment
increment: Patch

branches:
  develop:
    regex: ^develop
    source-branches: [ 'develop' ]
    tag: 'dev'
    
  feature:
    regex: ^feature?[/-]
    source-branches: [ 'develop' ]
    tag: useBranchName
    increment: 'Inherit'
